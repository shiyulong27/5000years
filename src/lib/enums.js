/**
 * 数据枚举的单一来源。
 *
 * 校验器与工具栏共用这里的定义——两处各写一份的代价已经付过一次：
 * category 收缩后校验器改了、工具栏没改，界面上留着三个永远筛不出
 * 东西的复选框。
 */

/**
 * 事件类别。互斥，用于工具栏的主筛选。
 *
 * 刻意保持少而粗。「灾异」拆开了——「灾」是实事，「异」是天人感应的
 * 解释框架，混在一类里既不好筛也不好写，故只留「灾害」。
 * 「科技」条目稀少且多与文化交织，并入文化。
 *
 * 更细的划分（制度、宗教、科技、民族、边疆…）走自由的 tags 字段：
 * tags 不做枚举校验、不做筛选开关，供将来的搜索与交叉引用使用。
 */
export const CATEGORIES = ['政治', '战争', '文化', '经济', '灾害']

/**
 * 可信度。
 *
 * 「传说」与「有争议」性质不同：有争议是学界对史实有分歧，
 * 传说是根本不以信史论（三皇五帝、涿鹿之战）。
 */
export const CONFIDENCES = ['确定', '存疑', '有争议', '传说']

/**
 * 人物的领域。
 *
 * 取值刻意少而粗——细分（诗人/词人/散文家）在长卷这个尺度上无意义，
 * 且会让按领域分色失去辨识度。
 */
export const FIELDS = ['文学', '思想', '政治', '军事', '科技', '艺术']
